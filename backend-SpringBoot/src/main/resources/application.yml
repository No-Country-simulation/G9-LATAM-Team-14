spring:
  application:
    name: backend-SpringBoot

  datasource:
    url: ${DB_URL:jdbc:oracle:thin:@fincoachdb_high?TNS_ADMIN=${TNS_ADMIN_PATH:src/main/resources/wallet}}
    username: ${DB_USER:ADMIN}
    password: ${DB_PASS:TuPasswordSegura2026!}
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect

  cache:
    type: ${CACHE_TYPE:simple}

jwt:
  secret: ${JWT_SECRET:un_secret_temporal_para_pruebas_123456}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

app:
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:4200}

ds:
  service:
    url: ${DS_SERVICE_URL:http://127.0.0.1:8000}